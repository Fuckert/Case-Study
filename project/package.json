{
  "name": "haw-landshut-portal",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "lint": "eslint . --ext js --report-unused-disable-directives --max-warnings 0",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "devDependencies": {
    "vite": "^5.4.2",
    "eslint": "^8.57.0"
  }
}